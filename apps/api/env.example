# Application Configuration
NODE_ENV=development
PORT=3000
HOST=localhost

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-key
JWT_ACCESS_EXPIRE=15m
JWT_REFRESH_EXPIRE=30d

# Database Configuration (PostgreSQL)
# Option 1: Use connection string (recommended for production)
# For local PostgreSQL:
DATABASE_URL=postgresql://username:password@localhost:5432/notify_chat

# For Supabase - Direct Connection (port 5432) - Used for migrations
# Get this from: Supabase Dashboard > Project Settings > Database > Connection String > Direct Connection
# DATABASE_URL=postgresql://postgres:[YOUR_PASSWORD]@db.xxxxx.supabase.co:5432/postgres?sslmode=require

# For Supabase - Session Pooler (port 6543) - Used for application runtime (recommended)
# Get this from: Supabase Dashboard > Project Settings > Database > Connection String > Session Pooler
# This provides better connection management and is optimized for serverless/server applications
# DATABASE_POOLER_URL=postgresql://postgres.[PROJECT_REF]:[YOUR_PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?sslmode=require
# Or if using direct connection URL format, it will be auto-converted to pooler:
# DATABASE_POOLER_URL=postgresql://postgres:[YOUR_PASSWORD]@db.xxxxx.supabase.co:6543/postgres?sslmode=require

# Note: If your password contains special characters, URL-encode them:
#   @ becomes %40, # becomes %23, / becomes %2F, % becomes %25, etc.
# Example: postgresql://postgres:p%40ssw%23rd@db.xxxxx.supabase.co:5432/postgres?sslmode=require

# SSL Configuration
# Supabase automatically enforces SSL. The connection strings above include sslmode=require
# For other PostgreSQL servers, set DB_SSL=true to enable SSL

# Option 2: Use individual config (fallback if DATABASE_URL not provided)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=postgres
# DB_PASSWORD=password
# DB_NAME=notify_chat
DB_SYNCHRONIZE=true
DB_LOGGING=true

# Redis Configuration
# Option 1: Use connection string (recommended for production)
REDIS_URL=redis://localhost:6379/0
# For Redis with password: redis://:password@localhost:6379/0
# For Redis SSL: rediss://:password@localhost:6379/0

# Option 2: Use individual config (fallback if REDIS_URL not provided)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# WebSocket Configuration
WS_CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=info
