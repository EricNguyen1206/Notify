# ==============================================
# Notify Chat Application Environment Variables - DEVELOPMENT
# ==============================================

# Application Configuration
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1

# Frontend Configuration - Direct container communication (for debugging)
# Use this configuration if you need to bypass nginx proxy
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_WS_URL=ws://localhost:8080

# Backend Configuration
NOTIFY_HOST=0.0.0.0
NOTIFY_PORT=8080
NOTIFY_JWT_SECRET=dev-secret-change-in-production
NOTIFY_JWT_EXPIRE=24h

# CORS Configuration (for development)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost

# Database Configuration (PostgreSQL)
POSTGRES_URL=postgres://postgres:postgres@db:5432/postgres?sslmode=disable
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Development Notes:
# - This configuration uses direct backend access (localhost:8080)
# - For production, use the main .env file with nginx proxy
# - To use this file: docker compose --env-file .env.development up
